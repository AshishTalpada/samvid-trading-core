# SOVEREIGN TRADING SYSTEM: FIX PLAN & IMPLEMENTATION ROADMAP
**Date:** May 11, 2026
**Goal:** Harden the system from prototype to safe pilot-ready production (paper-first)

---

## OVERALL PHASES (12+ weeks minimum)

1. Phase 0 — Immediate Safety (1 week)
2. Phase 1 — Structural Refactor (2-4 weeks)
3. Phase 2 — Automated Tests & CI (2-3 weeks)
4. Phase 3 — Observability & Monitoring (1-2 weeks)
5. Phase 4 — Security Hardening (1-2 weeks)
6. Phase 5 — Resilience & Chaos Engineering (2-3 weeks)
7. Phase 6 — Paper Trading Run & Pilot (4+ weeks)

---

## PHASE 0 — IMMEDIATE SAFETY (Days 0-7)

Purpose: Stop bleeding, avoid further live money risk.

Actions (urgent, must do today):
- A0.1: Force global `PAPER_MODE` flag and ensure it cannot be flipped by agents.
  - Implement environment variable `TRADING_MODE` ("paper"/"live") and enforce it at startup before agents initialize.
- A0.2: Add a global `EMERGENCY_HALT()` function that: logs, notifies Telegram, stops new order submissions, and sets `TradingStateManager.halt()`.
- A0.3: Lock critical config via read-only policy during runtime (no agent runtime mutations of risk params).
- A0.4: Add `assert` checks in reconciliation steps to refuse blind trades when data is stale or broker is disconnected.
- A0.5: Switch all production runtimes to `PAPER` immediately (do not accept risk until Phase 3).

Deliverables:
- `src/config.py` updated: environment-driven, `TRADING_MODE` enforcement
- `src/telemetry.py`: emergency alert helper
- `src/main.py`: startup safety check

---

## PHASE 1 — STRUCTURAL REFACTOR (Weeks 1-4)

Purpose: Break `brain.py` into maintainable modules and create clear boundaries.

Actions:
- B1.1: Split `src/brain.py` into modules (create `src/brain/` package):
  - `coordinator.py` — Orchestration loop, agent lifecycle
  - `reconciliation.py` — Position reconciliation, DB writes
  - `risk_manager.py` — Drawdown ladder, ConsecutiveLossTracker, risk policies
  - `execution.py` — Order placement, retry policies, signing
  - `state_manager.py` — TradingStateManager, persistent checkpoints
  - `api.py` — External API server endpoints
- B1.2: Replace global mutable state with an explicit `Context` dataclass passed between modules.
- B1.3: Add explicit interfaces (abstract base classes) for Agents to implement: `evaluate() -> Vote` with typed payloads.
- B1.4: Add schema validation for bus messages (use `pydantic` models) and enforce on publish/subscribe.
- B1.5: Add unit-level logging contexts (request IDs, trace IDs) for cross-module correlation.

Deliverables:
- `src/brain/__init__.py`, `coordinator.py`, `reconciliation.py`, `risk_manager.py`, `execution.py`, `state_manager.py`
- Tests: Unit tests for each new module's public interface

---

## PHASE 2 — AUTOMATED TESTS & CI (Weeks 3-6)

Purpose: Catch regressions early and provide confidence.

Actions:
- C2.1: Add `pytest` to project and baseline tests.
- C2.2: Create unit tests for: reconciliation, drawdown ladder, position sizing, bus schema validation, vault integration.
- C2.3: Add integration tests using mocked broker (`ibkrstub` / `mt5stub`) that test full trade lifecycle.
- C2.4: Add regression tests for previously failing bugs ("bool is not callable").
- C2.5: Add CI pipeline (`.github/workflows/ci.yml`) to run `pytest`, `ruff`, and `mypy` on PRs.

Deliverables:
- `tests/unit/` with coverage target `>80%` for core modules
- `.github/workflows/ci.yml`

---

## PHASE 3 — OBSERVABILITY & MONITORING (Weeks 4-6)

Purpose: Make the system observable; know when it's sick.

Actions:
- D3.1: Instrument code with `opentelemetry` for tracing (Async context), bridge to `Jaeger`.
- D3.2: Export metrics (`prometheus_client`) for:
  - Orders/sec, failures/sec
  - Reconciliation latency
  - Broker connectivity status
  - Memory/CPU
- D3.3: Centralize logs with structured JSON and integrate with `Loki`/`Grafana`.
- D3.4: Create alerting rules and Uptime checks (PagerDuty integration)
- D3.5: Add dashboard templates in Grafana for SREs and Trading Ops

Deliverables:
- `src/telemetry.py` etc., Grafana dashboard JSON, Prometheus scrape config

---

## PHASE 4 — SECURITY HARDENING (Weeks 5-7)

Purpose: Protect keys, enforce least privilege, and enable audit trails.

Actions:
- E4.1: Enforce Vault usage; fail startup if keys are missing from Vault.
- E4.2: Encrypt SQLite DB at rest with `sqlcipher` or use encrypted filesystem.
- E4.3: Move signing to HSM or YubiKey for production.
- E4.4: Require multi-sig for production risk mode flips.
- E4.5: Add access logs and immutable audit log for all order approvals

Deliverables:
- `scripts/advanced_setup.py` updated; DB encryption enforced; HSM instructions

---

## PHASE 5 — RESILIENCE & CHAOS ENGINEERING (Weeks 6-9)

Purpose: Ensure system survives partial failures.

Actions:
- F5.1: Add circuit breakers around each agent with fallback strategies (e.g., neutral vote when agent hangs).
- F5.2: Implement backpressure on `SharedIntelligenceBus`—reject new publishes when queue full with reason codes.
- F5.3: Add retry/backoff with idempotency keys for order submissions.
- F5.4: Add persistent checkpointing of trading state (nightly and every 5 minutes) with atomic snapshots.
- F5.5: Run chaos tests: drop broker connection, spike CPU, corrupt DB rows, and verify recovery.

Deliverables:
- `src/safety/` with circuit breaker, idempotency, DLQ improvements
- Chaos engineering playbook

---

## PHASE 6 — PAPER TRADING RUN & PILOT (Weeks 10+)

Purpose: Prove stability before real capital on board.

Actions:
- G6.1: Run for 30+ days in paper mode under real market conditions.
- G6.2: Monitor P&L, reconciliation errors, and all alerts.
- G6.3: Harden gaps discovered during the run.
- G6.4: Small live pilot (0.1% capital) only after 30-day green run.

Deliverables:
- 30-day paper run report, issues list, pilot checklist

---

## QUICK WINS (Days 0-3)
- Add global `PAPER_MODE` enforced from environment.
- Fail-safe `EMERGENCY_HALT()` with Telegram/pager alert.
- Add `assert` guards in `_reconcile_broker_positions` to avoid calling non-callables.
- Stop agents from changing global config at runtime.
- Add a top-level test that imports `TradingBrain` and runs a smoke check.

---

## RESOURCE ESTIMATE
- Total Engineering Weeks: 12–16
- Team: 1 Senior Backend Engineer, 1 SRE/DevOps, 1 QA Engineer, 1 Security Consultant (part time)
- Cost Estimate: Depends on hourly rates, but assume $80–$200k for a proper hardening

---

## NEXT ACTIONS (If you want me to proceed)
- I can prepare the `PAPER_MODE` patch and `EMERGENCY_HALT()` implementation now.
- Or I can generate the initial CI workflow and a starter set of unit tests.

Which should I do first? (I recommend the safety patch: `PAPER_MODE + EMERGENCY_HALT()`)
