# ═══════════════════════════════════════════════════════════════════
# UNIFIED TRADING SYSTEM v1.0-beta — REQUIREMENTS
# Install: pip install -r requirements.txt
# Python: 3.10, 3.11, or 3.12 ONLY (not 3.13)
# ═══════════════════════════════════════════════════════════════════

# ── Multi-AI Orchestration ──────────────────────────────────────────
crewai==0.80.0
crewai-tools==0.14.0

# ── AI Providers (latest March 2026 SDKs) ──────────────────────────
anthropic==0.49.0              # Claude Opus 4.6 + Sonnet 4.6
openai==1.68.0                 # GPT-5.4 + GPT-5.4 Thinking
google-genai==1.0.0            # Gemini 3.1 Pro Preview

# ── LangChain Wrappers ──────────────────────────────────────────────
langchain==0.3.0
langchain-anthropic==0.3.0
langchain-openai==0.3.0
langchain-google-genai==2.1.0
langchain-community==0.3.0

# ── Trading Brokers ─────────────────────────────────────────────────
ib_insync==0.9.86              # IBKR TWS / IB Gateway API
MetaTrader5==5.0.4522          # MT5 Python API (Windows only)

# ── Market Data ─────────────────────────────────────────────────────
yfinance==0.2.54               # Historical OHLCV data
ta==0.11.0                     # Technical indicators (RSI, ATR, MACD)
finnhub-python==2.4.20         # Real-time news + market data
openbb>=4.0.0                  # Bloomberg Terminal alternative — multi-source financial data

# ── Data & Math ─────────────────────────────────────────────────────
numpy==1.26.0
pandas==2.2.0
polars==1.2.1                  # High-performance HFT data processing
scipy==1.13.0                  # Statistical functions, confidence intervals
scikit-learn==1.5.0            # Regime classification, clustering
numba>=0.59.0                  # JIT compiler — 50-100x speedup on indicator math
hmmlearn>=0.3.0                # Hidden Markov Model for regime detection (RegimeFilter)

# ── Async & Networking ──────────────────────────────────────────────
aiohttp==3.10.0                # Async HTTP for market data feeds
websockets==13.0               # WebSocket connections (broker feeds)
asyncio-mqtt==0.16.0           # MQTT for IPC between processes (optional)
httpx>=0.25.0                  # Async HTTP client (SwarmPredictor / MiroFish)

# ── Alerts ──────────────────────────────────────────────────────────
python-telegram-bot==21.0      # Telegram bot for trade alerts + DMS
twilio==9.0.0                  # SMS alerts (optional, has cost)

# ── Infrastructure ──────────────────────────────────────────────────
python-dotenv==1.0.1           # Load .env API keys
pydantic==2.8.0                # Data models + validation
structlog==24.4.0              # Structured logging
rich==13.7.1                   # Beautiful terminal output
tenacity==9.0.0                # Retry logic for API calls
schedule==1.2.2                # Weekly/monthly calibration scheduler
psutil==6.0.0                  # RAM/CPU detection + process monitoring
pytz==2024.1                   # Timezone handling (Prague midnight for FTMO)
keyring==24.3.1                # Windows Credential Manager access
cryptography==42.0.5           # Column-level database encryption

# ── Database ────────────────────────────────────────────────────────
# sqlite3 is built-in to Python — no install needed
psycopg2-binary==2.9.9           # QuestDB PostgreSQL wire queries (brain OHLCV reads)

# ── Testing ─────────────────────────────────────────────────────────
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# ── Institutional Dashboard & API Bridge ────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
zstandard>=0.22.0
winloop>=0.1.0; sys_platform == 'win32'
llama-cpp-python>=0.2.56
